# Scripts

Put repeatable local prototype data conversion scripts here.

The first expected script should convert the GeoPackage in `data/source/` into `public/data/projects.geojson`, validating and normalizing properties against `RestorationProjectSubmission` in `schemas/hrl/linkml/hrl_restoration_project.yaml`.
